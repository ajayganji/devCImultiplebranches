apiVersion: apps/v1
kind: Deployment 
metadata: 
  name: deployment
spec: 
  replicas: 2
  selector:
    matchLabels:
      app: myapp3
  template:  
    metadata: 
      name: myapp3-pod
      labels: 
        app: myapp3 
    spec:
      containers:
        - name: myapp3-container
          image: ganjiacr.azurecr.io/${{variables.container_qa_repo_name}}:$(tag)
          ports: 
            - containerPort: 80
      imagePullSecrets:
        - name: acr-secret-02